# Changelog

## 0.0.1
- Initial GitHub release.
- Indoor/outdoor positioning controller with deterministic trace recording + replay.
- Indoor PDR engine (step detection + step length estimation).
- Barometer altimeter + floor change detection + floor height model.
- Indoor snapping (corridor/graph) and transition snapping utilities.
- Example Flutter app (live session, trace tools, scenarios).
